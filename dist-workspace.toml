[workspace]
members = ["cargo:."]

# Config for 'dist'
[dist]
# The preferred dist version to use in CI (Cargo.toml SemVer syntax)
cargo-dist-version = "0.32.0"
# CI backends to support
ci = "github"
# The installers to generate for each app
installers = ["shell", "powershell"]
# Target platforms to build apps for (Rust target-triple syntax)
targets = ["aarch64-apple-darwin", "aarch64-unknown-linux-gnu", "x86_64-apple-darwin", "x86_64-unknown-linux-gnu", "x86_64-pc-windows-msvc"]
# Path that installers should place binaries in
install-path = "CARGO_HOME"
# Whether to install an updater program
install-updater = false
# Build the full 5-target matrix on pull requests (without publishing anything).
# The release workflow is otherwise only exercised by pushing a v* tag, which is
# where v0.4.0 died on the Windows build — and where rust-toolchain.toml now
# governs which compiler each runner uses. This makes that rehearsable.
pr-run-mode = "upload"

# Native ARM runner — whisper.cpp is vendored C++ and cross-compiling that
# stack from x86_64 is still not worth the pain
[dist.github-custom-runners]
aarch64-unknown-linux-gnu = "ubuntu-24.04-arm"
